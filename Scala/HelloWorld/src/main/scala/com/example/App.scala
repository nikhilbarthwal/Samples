package com.example

object App {
  def main(args: Array[String]): Unit = {
    println("Hello, Scala 3 with Maven!")
  }

  /* public static void main (String[] args) {
    // Create two Apint numbers
    Apint a = new Apint("123456789012345678901234567890");
    Apint b = new Apint("987654321098765432109876543210");

    // Compute GCD using the gcd() method of Apint
    Apint gcd = a.gcd(b);

    // Print the result
    System.out.println("GCD of " + a + " and " + b + " is: " + gcd.toString());
  } */
}
